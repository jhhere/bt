<% if goal == @current_goal %>
<ol class="current_goal">
  <div class="bg_fade"></div>
<% else %>
<ol>
<% end %>
  <%= goal.goal %> <%= link_to "(remove)", goal, method: :delete, data: { confirm: "Are you sure?" } %>
</ol>
